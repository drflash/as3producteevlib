# -----------------------------------------------------------------
# User-Defined Paths
#
# Modify these path values to reflect paths on your system
# -----------------------------------------------------------------

flex.sdkVersion = 3.5.0
flex.path = C:/Program Files/Adobe/Adobe Flash Builder 4 Plug-in
flex.sdkPath = ${flex.path}/sdks/${flex.sdkVersion}
flex.binaryPath = ${flex.sdkPath}/bin
flex.asdoc = C:/Flex3/sdks/4.0.0/bin/asdoc.exe
FLEX_HOME = ${flex.sdkPath}

# The debug player is necessary here because it writes trace statements to a flashlog.txt
# file.  This allows us to examine the .txt file and determine the status of unit tests
# in an automated fashion.
flashDebugPlayer.exe = C:\\Program Files\\Adobe\\Adobe Flash Builder 4 Plug-in\\player\\win\\10.1\\FlashPlayerDebugger.exe

# -----------------------------------------------------------------
# File Names - DO NOT MODIFY
# -----------------------------------------------------------------
testRunner.dir = .
testRunner.name = ProducteevTestRunner

library.name = as3producteevlib

# -----------------------------------------------------------------
# Project Paths - DO NOT MODIFY
# -----------------------------------------------------------------
build.dir = ${basedir}/build
libs.dir = ${basedir}/libs
src.dir = ${basedir}/src
tests.dir = ${basedir}/tests
bin.dir = ${basedir}/bin
docs.dir = ${basedir}/docs